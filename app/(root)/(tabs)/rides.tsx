import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
// Was initially called history
const Rides = () => {
  return (
    <SafeAreaView>
      <Text>history</Text>
    </SafeAreaView>
  )
}

export default Rides